import { Typography } from "../../components/Typography/Typography";

export default function Pedido(){

    return (
        <div className="w-full flex items-center justify-center">
            <Typography color='black'> Pedido </Typography>
        </div>
    )
}